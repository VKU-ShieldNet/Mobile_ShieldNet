# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

.env

# -----------------------------
# Senior-level additions
# -----------------------------
# Keep local configuration and secrets off the repo
android/local.properties
key.properties
*.jks
*.keystore

# Firebase / Google services local keys (usually safe to keep but often moved to CI)
**/google-services.json
**/GoogleService-Info.plist

# Flutter plugins + generated files
/.flutter-plugins
/.flutter-plugins-dependencies
/.packages
/GeneratedPluginRegistrant.java
/GeneratedPluginRegistrant.m

# Xcode
ios/Runner.xcodeproj/xcshareddata/
ios/Runner.xcodeproj/project.xcworkspace/
ios/Runner.xcworkspace/
ios/Flutter/Generated.xcconfig
.ios/Flutter/Flutter.framework
Podfile.lock
Pods/

# Android
/android/.gradle/
/android/.idea/
/android/local.properties
/android/app/*.keystore

# CI, caches and editor configs
.circleci/
.github/
/.idea/
.vscode/
/*.keystore

# Native build artifacts
/build/
/android/**/build/
/ios/**/build/

# macOS
.DS_Store
*.moved-aside
*.xcuserstate

# Log files
*.log

# LFS and large asset management
# Add `*.png` to LFS config if you want to track large images instead of ignoring them

# Keep local environment variables out
.env*
secrets.json

# Other recommended ignores
pubspec.lock
# Note: For apps we generally commit pubspec.lock for reproducibility. Remove the line above if you want to keep it in VCS.

# Local caches
.dart_tool/
.pub/
.pub-cache/

# Optional: keep these files out of VCS on developer machines only
# Add to .git/info/exclude or global gitignore
# local-override.yaml